server {
    listen 8080;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html =404;
    }

    location /config.json {
        alias /tmp/config.json;
        default_type application/json;
    }
}